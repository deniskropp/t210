# Exceptions API Reference

Error hierarchy for the Klipper SDK.

**Path**: `src.shared.exceptions` (and projected `src.core.domain.exceptions`)

*Note: Currently, the SDK uses standard Python exceptions in some places, but `KlipperSDKError` is the precise base class.*

## Class: `KlipperSDKError`

Base exception for all SDK-related errors.
